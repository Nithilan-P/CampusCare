import './App.css'

function App() {
  return (
    <main className="app-shell">
      <section className="app-card">
        <p className="mb-3 text-sm font-semibold uppercase tracking-[0.3em] text-primary">
          CampusCare
        </p>
        <h1 className="mb-4 text-3xl font-semibold text-text-primary">
          Campus issue reporting is being scaffolded.
        </h1>
        <p className="text-base text-text-secondary">
          The client and server foundations are in place for Phase 1, and the app is ready for the next build steps.
        </p>
      </section>
    </main>
  )
}

export default App
